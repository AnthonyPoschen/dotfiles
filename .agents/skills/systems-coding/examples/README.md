# Systems Coding Examples

Use this index to load only the smallest example set needed for a rule decision.

## Files

- `control-flow.md` - early returns, guard clauses, avoid nested `if` chains
- `functions.md` - decomposition for long functions and orchestration pattern
- `variables-magic-values.md` - dependency injection and literal consolidation
- `comments.md` - phase comments, docs, and tagged comment usage

## Lookup Guidance

1. Start from `../SKILL.md` and match the rule category.
2. Open one topic file only when rule application is unclear.
3. Avoid loading all topic files by default.
