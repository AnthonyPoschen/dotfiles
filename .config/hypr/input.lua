-- Mouse / cursor overrides from the pre-4.0 input.conf.
-- Keyboard options (repeat delay, compose-only caps) are left for a later pass.

hl.env("HYPRCURSOR_THEME", "Bibata-Original-Amber")
hl.env("HYPRCURSOR_SIZE", "28")
hl.env("XCURSOR_THEME", "Bibata-Original-Amber")
hl.env("XCURSOR_SIZE", "28")

hl.config({
  input = {
    sensitivity = 0,
    accel_profile = "flat",
  },
})
